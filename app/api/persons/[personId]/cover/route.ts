import { requirePersonOwner } from "@/lib/permissions";
import { getPhotosBucket } from "@/lib/r2";
import { getSession } from "@/lib/session";
export async function GET(request:Request,{params}:{params:Promise<{personId:string}>}){const session=await getSession();if(!session)return new Response("Unauthorized",{status:401});const p=await requirePersonOwner((await params).personId,session.user.id);if(!p.coverKey)return new Response("Not found",{status:404});const obj=await (await getPhotosBucket()).get(p.coverKey,{onlyIf:{etagDoesNotMatch:request.headers.get("if-none-match")??undefined}});if(!obj)return new Response("Not found",{status:404});return new Response(obj.body,{headers:{"Content-Type":"image/webp","Cache-Control":"private, max-age=3600","ETag":obj.httpEtag,"X-Content-Type-Options":"nosniff"}})}
