import { and, eq } from "drizzle-orm";
import { getDb } from "@/db";
import { timelines, yearPhotos } from "@/db/schema";
import { getPhotosBucket } from "@/lib/r2";
import { getSession } from "@/lib/session";
export async function GET(request:Request,{params}:{params:Promise<{photoId:string}>}){const session=await getSession();if(!session)return new Response("Unauthorized",{status:401});const {photoId}=await params;const url=new URL(request.url);const variant=url.searchParams.get("variant")==="large"?"large":"thumbnail";const [row]=await (await getDb()).select({photo:yearPhotos,person:timelines}).from(yearPhotos).innerJoin(timelines,and(eq(yearPhotos.personId,timelines.id),eq(timelines.ownerId,session.user.id))).where(eq(yearPhotos.id,photoId)).limit(1);if(!row)return new Response("Not found",{status:404});const key=variant==="large"?row.photo.largeKey:row.photo.thumbnailKey;const obj=await (await getPhotosBucket()).get(key,{onlyIf:{etagDoesNotMatch:request.headers.get("if-none-match")??undefined}});if(!obj)return new Response("Not found",{status:404});if(obj instanceof Response)return obj;return new Response(obj.body,{headers:{"Content-Type":"image/webp","Cache-Control":"private, max-age=3600","ETag":obj.httpEtag,"X-Content-Type-Options":"nosniff"}})}
